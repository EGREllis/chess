services:

  backend:
    build:
      context: ./backend
    ports:
     - 8080:8080

  frontend:
    build:
      context: ./frontend
    ports:
     - 3000:3000

  nginx:
    build:
      context: ./nginx
    ports:
     - 80:80


